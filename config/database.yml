# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
development:
    adapter: mysql
    encoding: utf8
    reconnect: false
    database: references_development
    username: referencesuser
    password: pointone
    socket: /tmp/mysql.sock
    pool: 5
    timeout: 5000

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
    adapter: mysql
    encoding: utf8
    reconnect: false
    database: references_test
    username: referencesuser
    password: pointone
    socket: /tmp/mysql.sock
    pool: 5
    timeout: 5000

production:
    adapter: mysql
    encoding: utf8
    reconnect: false
    database: references_production
    username: referencesuser
    password: pointone
    socket: /tmp/mysql.sock
    pool: 5
    timeout: 5000
